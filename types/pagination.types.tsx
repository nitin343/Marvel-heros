export interface paginationDataType {
    paginationProps: {
        total: number;
        limit: number;
        count: number;
        offset: number;
        pageSize: number;
      };
}

